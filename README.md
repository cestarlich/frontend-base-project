## Using gulp for compile scss to CSS

For compile scss into css follow this steps:

1 - Execute "npm install" shell command on the root directory.
2 - Execute "gulp" shell command on the root directory.